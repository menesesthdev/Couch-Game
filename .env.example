# Copie para .env (não versionado). O docker compose lê este arquivo automaticamente.
HENRIKDEV_API_KEY=sua-chave-aqui

# Rodando o backend fora do Docker, prefira:
#   dotnet user-secrets set "HenrikDev:ApiKey" "sua-chave" --project backend/src/CoachGame.Api
